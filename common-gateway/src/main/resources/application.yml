server:
  port: 8089
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
  application:
    name: common-gateway
eureka:
#  instance:
#    ip-address: true
  client:
    service-url:
      defaultZone: http://172.17.0.4:7001/eureka/